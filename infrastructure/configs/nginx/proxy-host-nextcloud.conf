# Nginx Proxy Manager — Nextcloud Proxy Host Config
# Source domain: raspberrypi-1.tail2767bf.ts.net
# Forward: http://100.66.68.83:8080

# Details tab settings:
# Domain Names: raspberrypi-1.tail2767bf.ts.net
# Scheme: http
# Forward Hostname/IP: 100.66.68.83
# Forward Port: 8080
# Cache Assets: ON
# Block Common Exploits: ON
# Websockets Support: ON

# Advanced tab — paste this:
proxy_set_header Host $host;
proxy_set_header X-Real-IP $remote_addr;
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto https;
proxy_set_header X-Forwarded-Host $host;

# After saving, run these commands to configure Nextcloud:
# sudo docker exec --user www-data nextcloud-app-1 php occ config:system:set trusted_domains 1 --value="raspberrypi-1.tail2767bf.ts.net"
# sudo docker exec --user www-data nextcloud-app-1 php occ config:system:set overwrite.cli.url --value="https://raspberrypi-1.tail2767bf.ts.net"
# sudo docker exec --user www-data nextcloud-app-1 php occ config:system:set overwriteprotocol --value="https"
# sudo docker exec --user www-data nextcloud-app-1 php occ config:system:delete overwritewebroot
# sudo docker restart nextcloud-app-1
